{
  "name": "finalts-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:serial": "playwright test --workers=1",
    "test:smoke": "playwright test tests/smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3"
  }
}
